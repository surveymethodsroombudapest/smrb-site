---
layout: base.njk
---

{% set gobackTitle = "Publikációk" %}
{% set gobackUrl = "publ" | localisedLink(locale) %}

<div class="container-fluid">
	<div class="row justify-content-center">
		<div class="col-md-10 col-lg-8">
			<div class="article-header">
				{% goback gobackUrl,
				gobackTitle  %}
				<h1>{{ title }}</h1>
				{% if date %}
					<h6 class="subtitle">{{ date | date('YYYY. MM. DD.')}}</h6>
				{% endif %}
			</div>
			{{ content | safe }}
		</div>
	</div>
</div>